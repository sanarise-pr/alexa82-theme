const log = require('../logger/logger');
const palette = require('../palette');
const chroma = require('chroma-js');

log.title("Editor Warnings");

// log.val("contrast", chroma.contrast(bg, fg));

module.exports = {
  // "editorError.foreground": chroma('0000ff'),
  // "editorError.border": chroma('ff0000'),
  // "editorWarning.foreground": chroma('0000ff'),
  // "editorWarning.border": chroma('ff0000'),
  // "editorInfo.foreground": chroma('0000ff'),
  // "editorInfo.border": chroma('ff0000'),
};