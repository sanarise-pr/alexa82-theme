const log = require('../logger/logger');
const palette = require('../palette');
const chroma = require('chroma-js');

log.title("Text Colors");

// log.val("contrast", chroma.contrast(bg, fg));

module.exports = {
  // "textBlockQuote.background": chroma("00ffff"), 
  // "textBlockQuote.border": chroma("ff0000"),
  // "textCodeBlock.background": chroma("00ffff"),
  // "textLink.activeForeground": chroma("0000ff"),
  // "textLink.foreground": chroma("0000ff"),
  // "textPreformat.foreground": chroma("0000ff"),
  // "textSeparator.foreground": chroma("0000ff"),
};